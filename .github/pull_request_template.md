## Summary

<!-- one paragraph: what does this PR change? -->

## Agent adoption

<!--
The canonical home for this section is the COMMIT MESSAGE BODY of the feat:
commit, not this template — features here usually land with a local
fast-forward and never open a PR at all. Copying it here as well is fine and
harmless; the generator reads the commit first.

`.github/workflows/adoption_lint.yml` checks the commits, so a section that
exists only here will not satisfy the gate.

Delete this entire section for fix:/chore:/refactor:/docs: PRs unless they
change user-visible behavior. See CONTRIBUTING.md#agent-adoption.
-->

<!-- Prose: what existing projects need to know to take advantage of this change. -->

```prompt
<!-- Ready-to-paste prompt for an AI agent to adopt the change in a project. -->
```
